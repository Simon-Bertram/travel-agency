{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "@travel-kairos/api": "workspace:*",
    "@travel-kairos/auth": "workspace:*",
    "@travel-kairos/db": "workspace:*",
    "@travel-kairos/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "evlog": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@travel-kairos/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
